suite: Istio

templates:
  - gateway.yaml

tests:
  - it: points VirtualService to Service's external port
    set:
      service.externalPort: 1234
      istio.enabled: true
    asserts:
      - documentIndex: 1
        equal:
          path: spec.http[0].route[0].destination.port.number
          value: 1234
